
<div class="container" id="prindexcontainer">
 <div class="row prodrow">
 <% cache (cache_key_for_products) do %>
<%= render @products %> <!-- objects of the collection match name of the partial -->
<% end %> 
 </div>
<br>

<div class="submitbutton"><%= link_to 'New Product', new_product_path, class: 'btn btn-default', role:'button' %>
</div>
</div>

<!--<script>
$(document).on('ready page:load', function() {
    var heights = $(".prodcol").map(function() {
        return $(this).height();
    }).get(),

    maxHeight = Math.max.apply(null, heights);
    $(".prodcol").height(maxHeight);
});
</script>-->